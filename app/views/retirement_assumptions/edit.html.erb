<%= render partial: 'shared/header', locals: { title: 'Retirement', subtitle: 'Assumptions' } %>

<section class="section">
  <%= render 'form', retirement_assumption: @retirement_assumption %>
</section>
